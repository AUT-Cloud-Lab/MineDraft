# Usage
```
python main.py --help
usage: main.py [-h] [--script_name SCRIPT_NAME] [--config_path CONFIG_PATH] [--history_paths HISTORY_PATHS [HISTORY_PATHS ...]]

Extractors

options:
  -h, --help            show this help message and exit
  --script_name SCRIPT_NAME
                        Script name
  --config_path CONFIG_PATH
                        Config path
  --history_paths HISTORY_PATHS [HISTORY_PATHS ...]
                        Histories path
```

Currently available scripts are:
```
Available scripts:
	check_equality
```
